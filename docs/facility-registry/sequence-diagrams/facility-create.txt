@startuml
autonumber
skinparam responseMessageBelowArrow true

title "Create Facility"

Actor User as U
participant Client as FUI

box HFR Services
participant FacilitySvc as FS #gold
end box

box DIGIT Services #lightgray
participant "MDMS Service" as M #lightgreen
participant "IDGen Service" as IDG #lightgreen
participant "Boundary Service" as BS #lightgreen
participant "Workflow Service" as W #lightgreen
participant PersisterSvc as P #lightgreen
Database PostgreSQL as DB #gray
end box

U -> FUI: Fill in facility details

FUI -> FS++: POST /v2/facility/create\nwith RequestInfo and facilities[]

FS -> M++: Fetch MDMS data for:\n- facility_type\n- facility_category\n- facility_ownership\n- facility_region\n- solar_solution_design_type
return master data

FS -> FS: validate facility fields using master data

FS -> BS++: Validate facility boundary
return boundary validation result

FS -> IDG++: Generate facility_id
return facility_id

FS -> FS: enrich data

FS -> W++: initiate facility create workflow
return workflow status: INITIATED

' Removed Kafka
' FS -> K++: push facility payload to topic 'facility-create'
' return

FS -> P++: Persist facility data  ' Direct call to PersisterSvc (Separate Dev Effort, To be Implemented)
return

return 201 : Facility Created

' Removed Kafka consumption
' P -> K: consume facility-create topic
P -> DB: insert facility records

@enduml
